# Server port (default: 3100)
PORT=3100

# MyAgent SaaS API URL (the backend this MCP proxies)
MYAGENT_API_URL=https://myagent-app-dev.up.railway.app

# No API keys stored here — clients pass their own mak_ key as Bearer token
